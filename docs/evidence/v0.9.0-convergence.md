# Evidence v0.9.0 — convergence post-apply

Offline convergence evidence: passed

The protected offline gate verifies the convergence-v1 JSON schema, all four
stable outcome fixtures, selective readback, CLI exit-code behavior, partial
failure attachment and the secret-free read-only boundary. It also checks that
the MCP server has no mutation tool and that the v0.9 documentation and CI
gates are present.

The v0.9.0 contract does not claim automatic rollback, automatic retry or live
controller mutation evidence. A live controller lifecycle remains governed by
the existing resource-specific evidence suites.
