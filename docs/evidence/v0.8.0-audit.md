# Evidence v0.8.0 — audit

## Gate hors ligne

Offline audit evidence: passed.

Le gate vérifie le contrat JSON v1, les quatre états, les fixtures sans
secrets, la frontière read-only CLI/MCP, la présence des documents de
migration/release et l’absence d’outil de mutation MCP.

## Résultats couverts

| Cas | Résultat attendu | Preuve |
| --- | --- | --- |
| Déclaration et export identiques | `in-sync`, code `0` | fixture et test déterministe |
| Champ portable différent | `drifted`, code `1` | fixture et test de chemin de champ |
| Lecture impossible ou donnée incomplète | `unknown`, code `2` | fixture et test de panne/limitation |
| Capacité d’export absente | `unsupported`, code `2` | fixture et test de pré-vol |

Les rapports ne contiennent ni identifiants contrôleur, ni valeurs de WLAN,
ni noms de variables de secret, ni payloads bruts. Aucun contrôleur réel n’est
présenté comme preuve de couverture lorsque l’API ne permet pas une
observation complète.
