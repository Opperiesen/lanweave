# v1.0.0 contract evidence

Offline v1.0 evidence: passed

This evidence records the contract freeze before live release publication. It
covers the exported Python API, `py.typed`, version identifiers, machine-
readable schemas, CLI boundary, read-only MCP boundary and the v0.9.0 to v1.0.0
migration policy. Live controller and release artifacts are verified by their
protected workflows and linked from the final release issue.
